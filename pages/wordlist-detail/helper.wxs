// WXS 辅助函数：判断数组中是否包含指定值
function has(arr, val) {
  if (!arr) return false
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === val) return true
  }
  return false
}

module.exports = {
  has: has
}
